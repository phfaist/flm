[tool.poetry]
name = "llm"
version = "0.3.0alpha000007" # ALSO BUMP IN llm/llm/__init__.py
description = "Latex-Like Markup"
authors = ["Philippe Faist"]
license = "MIT"
include = ["llm/main/templates/**/*"]

[tool.poetry.scripts]
llm = 'llm.__main__:run_main'

[tool.poetry.dependencies]
python = "^3.8"
pylatexenc = {'git' = "https://github.com/phfaist/pylatexenc.git", branch = "devel" }
python-frontmatter = "^1.0.0"
PyYAML = ">=5.0"

[tool.poetry.dev-dependencies]
pytest = "^7.1.2"
ipython = "^8.3.0"
toml = "^0.10.2"

[tool.poetry.group.buildjslib]
optional = true

[tool.poetry.group.buildjslib.dependencies]
Transcrypt = ">=3.9.0"
PyYAML = ">=5.0"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"




[tool.pytest.ini_options]
testpaths = [
    "test",
]
