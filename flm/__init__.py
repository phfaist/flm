r"""
Flexible Latex-Like Markup (FLM) --- a Python library for parsing and
rendering LaTeX-like markup to HTML, plain text, LaTeX, and Markdown.

See :py:func:`~flm.flmenvironment.make_standard_environment` and
:py:func:`~flm.stdfeatures.standard_features` for the main entry points.
"""

# Version is hard-coded here to avoid any other dependencies (no importlib
# magic, no pkg_resources, no other stuff like that), especially for possible
# future use with transcrypt.

__version__ = "0.3.0alpha000030"

# ALSO BUMP VERSION NUMBER IN pyproject.toml !!
